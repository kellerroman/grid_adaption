PROGRAM TESTGRID
IMPLICIT NONE
REAL(KIND = 8), PARAMETER :: PI = 3.141592653589790D+00
INTEGER, PARAMETER :: NI = 10, NJ = 0, NK = 0, NOUT = 1
INTEGER :: AXSYM
INTEGER :: NSTEP
INTEGER :: NBLOCK
INTEGER :: NVAR
INTEGER :: NBASEVAR2


INTEGER :: I,J,K

REAL(KIND= 8), PARAMETER :: DX = 1.0D0, DY = 1.0D0
REAL(KIND = 8) :: WERT(NI)
OPEN(100,FILE="git.dat")
WRITE(100,'(2I5)') NI+1, NJ+1
   DO i= 0,NI
      WRITE(100,'(3(X,D20.13))') DX/NI * DBLE(i), 1.0D0
   END DO
CLOSE(100)

OPEN(100,FILE="git_cell.dat")
   DO i= 1,NI
!      WERT(i*j) = DBLE(i*j)
       WERT(i) = SIN(DBLE(1+(i-1)*2)*PI/DBLE(2*NI))
       WRITE(100,*) DBLE(1+(i-1)*2)*DX/DBLE(2*NI),",",WERT(i)
!       IF (I <= NI/2) THEN
!         WERT(i,j) = 0.0d0
!       ELSE
!         WERT(i,j) = 1.0d0
!       END IF
   END DO
CLOSE(100)
AXSYM=-1
NSTEP=0
NBLOCK=1
NVAR=1
NBASEVAR2=1
OPEN(100,FILE="avarout.ufo",FORM="UNFORMATTED")
WRITE(100) AXSYM,NSTEP,NBLOCK,NVAR,NBASEVAR2
WRITE(100) NI,1,1
WRITE(100) NOUT,NOUT,NOUT
WRITE(100) "TESTVAR             "
WRITE(100) (((WERT(i),i=1,NI),j=1,1),k=1,1)



CLOSE(100)

END PROGRAM
